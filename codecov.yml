comment: false
coverage:
  status:
    # Enforced patch status: changed lines must be at least 70% covered.
    # Set informational to true to report without blocking merges.
    patch:
      default:
        target: 70%
        informational: false
    project: false
